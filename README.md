# 🍽️ Chef's Choice

Chef's Choice is a React-based web application that uses **AI-powered recipe generation** to suggest recipes based on user input (ingredients). It integrates with **Hugging Face’s LLaMA 3.1 Instruct model** and displays responses in a clean, readable Markdown-based UI.
